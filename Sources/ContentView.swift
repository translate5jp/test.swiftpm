import SwiftUI

struct ContentView: View {
    var body: some View {
        Button("OK") {
            // TODO: add action
        }
        .buttonStyle(.borderedProminent)
        .controlSize(.large)
    }
}
